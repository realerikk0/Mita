prompt = """
You are testing the remote-only Biyan desktop application.

Step-by-step instructions:
1. Confirm the Biyan application is already open.
2. Open Providers and select the CI-provisioned OpenAI-compatible test provider.
3. Explicitly select its test model. Do not use or download a local model.
4. Start a new chat and send exactly: `Say exactly: remote chat works`.
5. Wait for the remote model response.
6. Verify no local runtime, model download, Hub, or embedding process was started.

Return {"result": True} only when the exact response is shown and the remote-only checks pass; otherwise return {"result": False}.

In all your responses, use only plain ASCII characters. Do NOT use Unicode symbols.
"""
