# 彼岩 Teams —— 产品评审与路线图

> 状态：评审完成，三个战略决定已拍板（2026-06-24）。本文件是分支 A / 分支 B 的依据。

## 已拍板的三个战略决定

1. **默认控制权契约 = 主人说了算（owner-authoritative）。**
   主人动过角色集或用自然语言写了约束，那就是这个团队；主持人（Host）可以重新分派任务，但**未经明确邀请不得自己造角色**。要让 LLM 帮忙组队时，提供一个显式、可见的"让主持人来组队"入口，而不是用静默默认覆盖所有人。
2. **目标用户 = 商业 / 研究通用用户**（分析师、研究、PM 做高风险活），不是开发者高玩。
   推论：去开发者黑话、明确化价值、空状态/默认都按通用用户设计。
3. **Mode 必须是"真有结构"的模式。**
   现有 5 个 mode（接力 / 圆桌 / 红队 等）应全部做成有强制调度/产出结构的真模式，成为真差异化，而不只是提示词味道。这是一个独立的 P1 级硬功能，不放进 P0。

## 一句话结论

引擎有真本事（多模型角色、唱反调的 Skeptic、花钱前的计划闸门），但外壳让它读起来像"更慢的聊天机器人"。两件事同时坏：用户**感知不到**多模型价值；用户**无法信任自己对它的掌控**。底子值得押注，多数修复是接线/文案，不是重构。

## 核心主题：配置"看着说了算，实际只是建议"

所有视角都落到同一条断层线——主人感觉失控。根因是默认姿态"除非触发两种隐藏语法之一，否则团队归 LLM 管"。决定 #1 就是要翻转这个姿态。次要主题：**产品算出来的东西却不告诉用户**（已有 `estimatedCostUsd`、`decision.reason`、token 数、预算耗尽状态，却只显示干巴标题、"$?"、把截断的运行标成"已完成"）。

## 问题优先级

| # | 问题 | 严重 | 修法 | 工作量 |
|---|---|---|---|---|
| 1 | 散文写的角色限制被静默忽略 | 致命 | 从散文识别限制意图；主人配过的角色集默认即白名单（`user_spec`） | M |
| 2 | 计划审批卡片只读，唯一手段=自由文本→LLM | 高 | 卡片可直接编辑（删/改角色、锁定团队）；批准=就跑显示的团队 | M |
| 3 | 场景剧本自动塞角色、还悄悄改 mode/template | 高 | 改成提示芯片（征求同意），绝不静默改 mode/template | M |
| 4 | 空状态死板；配置在用户发现前就锁了 | 致命 | 空状态做成引导（一键示例目标）；配置可编辑到计划审批前 | M |
| 5 | 角色消息不显示是哪个模型在说话 | 致命 | 每条角色消息打 provider/模型徽标（复用 `ProvidersAvatar`） | S |
| 6 | 最终答案看不出"团队凭啥赢过单模型" | 高 | 加"团队发现了什么"小条（Skeptic 风险、分歧调和） | M |
| 7 | 事件流只显示枚举，丢掉 reason/instruction | 高 | 渲染 `event.detail`、动作动词人话化 | S |
| 8 | 从不显示美元成本；token 表每轮清零 | 致命 | 显示成本（单次+累计）+审批卡估算；运行后刷新余额 | S–M |
| 9 | 没有重试/恢复，失败即死胡同 | 致命 | 终态给恢复卡（重试/继续），复用 #48 的解析回退模式 | M |
| 10 | 预算耗尽的运行被标成"已完成" | 高 | 独立状态"已到轮次上限——继续？" | M |
| 11 | "Mode"显眼却不控制执行（→ 决定 #3） | 高 | 把 5 个 mode 都做成真有结构的模式 | L |
| 12 | 没有控制权指示；协调者有 4 个名字 | 中 | "团队：锁定 vs 主持人可加人"开关；统一叫"主持人/Host" | S |
| 13 | zh-TW 缺整个计划审批界面（18 键） | 致命(台港) | 从 zh-CN 回填 + 键对齐校验 ✅ 已完成（分支 A） | S |
| 14 | 给非开发者上了开发者黑话 | 中 | "Build log/Artifacts/MVP guard" 换成中性词 | M |

## 路线图

- **现在（P0）= 分支 A「把已知的露出来」+ 分支 B「主人说了算」**
  - 分支 A（低风险、基本接线）：**#13 ✅**、#5、#7、#12、#8。
  - 分支 B（产品押注，依赖决定 #1）：#1 → #2，再 #4、#9。
- **接下来（P1）：** #3（剧本征求同意）、#6（价值小条）、#10（正确状态）、#11（5 个 mode 真结构化，决定 #3）、#14（去黑话）。
- **以后（P2）：** 团队预设、按角色成本档位、把编排提示词本地化（中文理解的真正根治，L）、换话题时重置陈旧计划。

## 相关源文件

`MitaTeamsWorkspace.tsx`、`lib/mita-teams-runtime.ts`、`types/mita-teams.ts`、`routes/threads/$threadId.tsx`、`locales/{en,zh-CN,zh-TW}/mita-teams.json`、`lib/new-chat.ts`、`lib/provider-balance-display.ts`、`containers/ProvidersAvatar.tsx`。

## 关联

- 先行 bug 修复：PR #48（`claude/biyan-teams-context-fix`）——已包含决定 #1 的第一步（场景注入抑制 = 主人配了团队就不自动加人）。
