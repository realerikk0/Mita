# `@biyan/core`

Private shared APIs and type definitions for Biyan and its bundled extensions.

```ts
import { events, MessageEvent } from '@biyan/core'
```

Build and test from this directory with `yarn build` and `yarn test`, or use the
root workspace commands. This package is not published to the public npm
registry.
